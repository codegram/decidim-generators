# frozen_string_literal: true

require "decidim/<%= engine_name %>/admin"
require "decidim/<%= engine_name %>/engine"
require "decidim/<%= engine_name %>/admin_engine"
require "decidim/<%= engine_name %>/component"

module Decidim
  # This namespace holds the logic of the `<%= engine_module_name %>` component. This component
  # allows users to create <%= engine_name %> in a participatory space.
  module <%= engine_module_name %>
  end
end
